class Product {
    constructor(name, price, description, availableStock) {
        this.name = name
        this.price = price
        this.description = description
        this.availableStock = availableStock
    }
}

export default Product